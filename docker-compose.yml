version: "3.8"
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./dev.db
      - SECRET_KEY=replace-me
      - CORS_ORIGINS=http://localhost:5173
    volumes:
      - ./:/app

# Example environment variables for local/demo deployment
# Copy to .env and customize before running

# Database connection (SQLite file by default)
DATABASE_URL=sqlite:///dev.db

# JWT secret (replace for production/demo sharing)
SECRET_KEY=replace-with-a-secret-key
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS origins (comma-separated). e.g. http://localhost:5173
CORS_ORIGINS=http://localhost:5173

# Optional: frontend build directory to serve static files from
# FRONTEND_BUILD_DIR=frontend/dist
